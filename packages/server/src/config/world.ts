/**
 * Мировые константы.
 *
 * Всё, что описывает «где» и «в каком виде» появляется игрок, живёт здесь,
 * а не разбросано по модулям: точку спавна меняют часто, и искать её по
 * коду не должно быть квестом.
 */

/** Точка появления нового персонажа. Пирс Дель-Перро: людное, узнаваемое место. */
export const DEFAULT_SPAWN = {
  x: -1604.3,
  y: -1015.4,
  z: 13.0,
  heading: 140.0,
  dimension: 0,
} as const;

/** Отдельное измерение для сцен, где игрок не должен видеть мир и других игроков. */
export const SCENE_DIMENSION_BASE = 100_000;

/** Модели фримода. Внешность накладывается поверх — см. PHASE 2 в roadmap. */
export const PLAYER_MODELS = {
  male: 'mp_m_freemode_01',
  female: 'mp_f_freemode_01',
} as const;

/** Стартовый капитал нового персонажа. Держим в одном месте с экономикой. */
export const STARTING_CASH = 500;
export const STARTING_BANK = 0;
